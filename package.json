{
  "name": "sandbox",
  "license": "MIT OR Apache-2.0",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "pnpm --filter ./frame-glue run watch & (vite & (cd subdomain && pnpm build && pnpm preview))",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "~5.7.2",
    "vite": "^6.1.0",
    "vite-plugin-pwa": "^0.21.1"
  },
  "dependencies": {
    "frame-glue": "link:frame-glue"
  }
}
